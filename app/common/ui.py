import streamlit as st

def inject_custom_css():
    """Добавление пользовательских CSS-стилей для ограничения ширины и центрирования контента"""
    st.markdown("""
    <style>
        .main > div {
            max-width: 800px;
            margin: 0 auto;
            padding: 1rem;
            box-sizing: border-box;
        }
        
        /* Стили для формы и всех элементов управления, чтобы они занимали полную ширину контейнера */
        .stButton button, div[data-testid="stFileUploader"], div[data-testid="stForm"] {
            width: 100%;
        }
        
        /* Улучшение читаемости и внешнего вида */
        h1, h2, h3 {
            margin-bottom: 1rem;
        }
        
        .row-widget.stButton {
            margin-top: 1rem;
        }
    </style>
    """, unsafe_allow_html=True)

def setup_page():
    """Настройка страницы и общих элементов интерфейса"""
    st.set_page_config(
        page_title="Краулер депрессивного контекста", 
        page_icon="😔", 
        layout="wide"
    )
    inject_custom_css()
    st.title("Краулер депрессивного контекста в социальных сетях")

def show_project_info():
    """Отображение информации о проекте"""
    with st.expander("О проекте", expanded=True):
        st.markdown("""
        ### Описание проекта
        
        Данный проект представляет собой инструмент для анализа активности пользователей социальной сети ВКонтакте с целью 
        выявления признаков депрессивного контекста в их публикациях. Краулер собирает данные профилей и посты пользователей, 
        а затем анализирует их с помощью модели машинного обучения, обученной на определение депрессивного контекста.
        
        ### Возможности краулера:
        - Сбор информации о профилях пользователей ВКонтакте
        - Извлечение публичных постов с текстом, датой публикации и реакциями
        - Анализ собранных данных на наличие признаков депрессии
        - Работа с несколькими пользователями последовательно
        - Поддержка различных форматов ввода (ID пользователя или полный URL)
        """) 